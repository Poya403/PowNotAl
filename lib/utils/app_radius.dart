import 'package:flutter/cupertino.dart';

class AppRadius{
  AppRadius._();

  static final radius2 = BorderRadius.all(Radius.circular(2));
  static final radius4 = BorderRadius.all(Radius.circular(4));
  static final radius6 = BorderRadius.all(Radius.circular(6));
  static final radius8 = BorderRadius.all(Radius.circular(8));
  static final radius10 = BorderRadius.all(Radius.circular(10));
  static final radius12 = BorderRadius.all(Radius.circular(12));
  static final radius16 = BorderRadius.all(Radius.circular(16));
  static final radius18 = BorderRadius.all(Radius.circular(18));
  static final radius20 = BorderRadius.all(Radius.circular(20));
  static final radius24 = BorderRadius.all(Radius.circular(24));
  static final radius28 = BorderRadius.all(Radius.circular(28));
  static final radius30 = BorderRadius.all(Radius.circular(30));
}